name: Java CI with Maven & JaCoCo

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar código del repositorio
        uses: actions/checkout@v3

      - name: Configurar Java 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Ejecutar Pruebas y Generar Reporte JaCoCo
        run: mvn clean test jacoco:report

      # Opcional: Esto sube el reporte HTML a GitHub para que lo puedas descargar y ver
      - name: Cargar Reporte de Cobertura como Artefacto
        uses: actions/upload-artifact@v3
        with:
          name: jacoco-report
          path: target/site/jacoco/
          retention-days: 5